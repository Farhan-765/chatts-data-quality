# Exploration Notebooks

Place the original monolithic exploration notebook here:

```
chatts_timeseer_extended.ipynb   (153 cells, 409 KB)
```

The original notebook is the single-file predecessor to this repository.
All production code has been extracted into `src/`, `scripts/`, and
`notebooks/final_experiments/`. The exploration notebook is archived here
for reference and reproducibility audits only — do not run it in CI.

## How to archive

```bash
cp /path/to/original/chatts_timeseer_extended.ipynb notebooks/exploration/
```

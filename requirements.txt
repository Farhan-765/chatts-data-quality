# Core ML
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Plotting
matplotlib>=3.8.0
seaborn>=0.13.0

# Notebook
jupyter>=1.0.0
ipywidgets>=8.1.0

# API calls (GPT-4o, Timeseer)
openai>=1.10.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
tqdm>=4.66.0

# NOTE: chronos-forecasting must be installed in a SEPARATE environment
# due to accelerate version conflict with ChatTS.
# See environment_chronos.yml
